// Begin Behavior 1
export function  ageCalc(input) {
  let userAge = 10;
  let planet = 1;
  if (planet = 1) {
  let mercAge = userAge / .24;
  return mercAge;
  }
  // let venusAge = userAge / .62;
  // let marAge = userAge / 1.88;
  // let jupAge = userAge / 11.86;
  return mercAge;
  }